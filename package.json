{
  "name": "claude-limit-monitor",
  "version": "0.1.0",
  "description": "Chrome extension to track usage limits across multiple Claude accounts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
